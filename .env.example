# Copy to .env and fill in. The real .env is not committed — see .gitignore.

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ksiegarnia

# express-session. Any long random string; the cookie is signed with it.
SESSION_SECRET=

# development | production
# production turns on compression and the secure cookie flag (HTTPS only).
NODE_ENV=development

PORT=3000
